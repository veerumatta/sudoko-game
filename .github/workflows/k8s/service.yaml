apiVersion: v1
kind: Service
metadata:
  name: sudoku-service
  namespace: sudoku
spec:
  type: NodePort
  selector:
    app: sudoku
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
